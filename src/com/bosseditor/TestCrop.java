package com.bosseditor;

//import com.open.crop.CropImageView3;

import android.os.Bundle;
import android.app.Activity;
import android.view.Menu;
import android.view.View;
import android.view.View.OnClickListener;
import android.widget.Button;
import android.widget.ImageView;

public class TestCrop extends Activity {
/*
	CropImageView3 iv;
	ImageView iv2;

	@Override
	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.activity_test_crop);
		Button btnScreenshot = (Button) findViewById(R.id.button1);
		Button btnCrop = (Button) findViewById(R.id.button2);

		iv = (CropImageView3) findViewById(R.id.crop_image_view);
		iv2 = (ImageView) findViewById(R.id.imageView2);

		iv.setVisibility(View.VISIBLE);
		iv2.setVisibility(View.GONE);
		btnCrop.setOnClickListener(new OnClickListener() {
			
			@Override
			public void onClick(View arg0) {
				iv.setVisibility(View.GONE);
				iv2.setVisibility(View.VISIBLE);
				iv2.setImageBitmap(iv.getCropImage());

			}
		});


	}

	@Override
	public boolean onCreateOptionsMenu(Menu menu) {
		// Inflate the menu; this adds items to the action bar if it is present.
		getMenuInflater().inflate(R.menu.test_crop, menu);
		return true;
	}

*/}
